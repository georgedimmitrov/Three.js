![Alt text](screenshots/screens.gif?raw=true 'Screenshot of final result')

## Setup

```bash
# Install dependencies (only the first time)
npm install

# Run the local server at localhost:8080
npm run dev
```
